
// import { AbstractControl, FormGroup, ValidationErrors, ValidatorFn } from '@angular/forms';

// export const passwordMatchValidator : ValidatorFn = (control: FormGroup): ValidationErrors | any => {
//   const Password : AbstractControl |any  = control.get('Password');
//   const ConfirmPassword : AbstractControl|any= control.get('ConfirmPassword');
//   if (Password.value && ConfirmPassword.value) {
//     return Password.value !== ConfirmPassword.value ? { 'passwordmatch': true } : null;
//   }
//   return null;
// };
